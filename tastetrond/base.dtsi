// layer names
#define L_QWERTY 0
#define L_NAV 1
#define L_NUM 2
#define L_SYM 3
#define L_FN 4
#define L_SYS 5
#define L_UC 6

// global settings
#define QUICK_TAP_MS 175

// key aliases
#define XXX &none
#define ___ &trans

// international chars
#define NO_AE RA(Q)
#define NO_OE RA(O)
#define NO_AA RA(A)

// homerow mods
#define KEYS_L LT0 LT1 LT2 LT3 LT4 LM0 LM1 LM2 LM3 LM4 LB0 LB1 LB2 LB3 LB4  // left hand
#define KEYS_R RT0 RT1 RT2 RT3 RT4 RM0 RM1 RM2 RM3 RM4 RB0 RB1 RB2 RB3 RB4  // right hand
#define THUMBS LH2 LH1 LH0 RH0 RH1 RH2                                      // thumbs

// key aliases
#define DSK_LEFT &kp LG(LA(H))
#define M_DSK_LEFT &kp LC(LA(H))
#define VOL_DOWN &hml LSHIFT C_VOL_DN
#define VOL_UP &hml LCTRL C_VOL_UP
#define DSK_RIGHT &hml LALT LG(LA(L))
#define M_DSK_RIGHT &kp LC(LA(L))
#define WM_RELOAD &kp LS(LC(LA(LG(R))))
